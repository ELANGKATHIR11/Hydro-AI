# PostgreSQL/PostGIS Connection
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/hydro_digital_twin

# Satellite Provider Configuration (for local/offline use, keep fallback or local path)
SAT_PROVIDER_PRIORITY=local,planetary_computer
DATA_DIR=data/sample
